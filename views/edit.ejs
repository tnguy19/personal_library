<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Personal Library</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <link rel="stylesheet" href="/styles/main.css" />
</head>
<body>
    <% if (locals.book){ %>
    <main>
        <h1>Edit Post</h1>
        <br>
        <div class="book-display">
            <form class="button mb-3" action="/edit/<%= book.id%>" method="POST">
                <input type="hidden" name="id" value="<%= book.id %>"> <!-- Hidden input to pass over  id -->
                <label for="title">Title: </label>
                <textarea cols="55" rows="1" name="title" wrap="physical"><%= book.title %></textarea><br> 
                <label for="isbn">ISBN: </label>
                <textarea cols="55" rows="1" name="isbn" wrap="physical"><%= book.isbn %></textarea><br>
                <label for="date">Date Read: </label>
                <textarea cols="50" rows="1" name="date" wrap="physical"><%= book.date_read %></textarea><br> 
                <label for="rating">Rating (out of 10): </label>
                <textarea cols="2" rows="1" name="rating" wrap="physical"><%= book.rating %></textarea><br>
                <label for="link">Amazon product link: </label>
                <textarea cols="41" rows="1" name="link" wrap="physical"><%= book.link%></textarea><br>
                <label for="comment">Comment: </label><br>
                <textarea cols="60" rows="10" name="comment" wrap="physical"><%= book.comment%></textarea><br>
                <button class="btn btn-light btn-outline-secondary rounded-pill px-3 d-inline-flex align-items-center">
                    Save Changes
                  </button>
            </form>
            <form class="button" action="/" method="GET">
                <button class="btn btn-outline-secondary rounded-pill d-inline-flex align-items-center">
                      Cancel
                </button>
            </form>
        </div>
        </main>
    <% } %>
</body>
</html>
